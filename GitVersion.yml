assembly-versioning-scheme: MajorMinorPatchTag
mode: ContinuousDelivery
branches:
  master:
    mode: ContinuousDelivery
    tag: alpha
    increment: none
  dev(elop)?(ment)?$:
    mode: ContinuousDelivery
    tag: alpha-rc
    increment: Patch
ignore:
  sha: []
