<UserControl x:Class="UCR.Views.Controls.PluginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:UCR.Views.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="100" d:DesignWidth="300" d:DataContext="{d:DesignData Source=/SampleData/ButtonToButtonSampleData.xaml}"
             x:Name="PluginViewName" Margin="0,0,0,5">
    
    <StackPanel x:Name="LayoutRoot" Height="auto" Background="{x:Static SystemColors.ControlBrush}" >
        <DockPanel Background="{x:Static SystemColors.ControlDarkBrush}" HorizontalAlignment="Left" Height="30" LastChildFill="False" VerticalAlignment="Top"
                   Width="{Binding ActualWidth, ElementName=PluginViewName}">
            <Label Content="{Binding Title}" VerticalAlignment="Center"/>
        </DockPanel>
        <Grid Margin="5" Height="auto">
            <ContentPresenter ContentTemplate="{Binding PluginViewContent, ElementName=PluginViewName}"/>
        </Grid>
    </StackPanel>
</UserControl>
