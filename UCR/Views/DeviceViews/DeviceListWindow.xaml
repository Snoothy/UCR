<Window x:Class="HidWizards.UCR.Views.DeviceViews.DeviceListWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:HidWizards.UCR.Views"
        xmlns:controls="clr-namespace:HidWizards.UCR.Views.Controls"
        mc:Ignorable="d"
        Title="Device groups" Height="600" Width="600">
    <Grid>
        <TabControl>
            <TabItem Header="Input groups">
                <controls:DeviceListControl DataContext="{Binding InputDeviceList}" />
            </TabItem>
            <TabItem Header="Output groups">
                <controls:DeviceListControl DataContext="{Binding OutputDeviceList}" />
            </TabItem>
        </TabControl>
    </Grid>
</Window>
